# Arşiv — v13.1 Mimar Denetimi (2026-08-06)

Bu dosya `docs/MASTER_PLAN.md`'nin 400 satır CI sınırı (#209) nedeniyle ayrılmış analiz kaydıdır. Canlı doktrin ve backlog tablosu MASTER_PLAN'dadır.

## 1. Ölçülen CI durumu — "%100 yeşil CI" iddiası yanlıştı

### 1a. İlk ölçüm (PR bağlamı)

PR #69 (`dependabot/github_actions/actions/setup-node-7`) check-run listesi, 2026-08-06T05:48Z:

| Kontrol                 | Sonuç       | Süre  |
| ----------------------- | ----------- | ----- |
| Secretlint Scan         | failure     | 2 sn  |
| Dependency Audit        | failure     | 2 sn  |
| Gitleaks Security Audit | failure     | 2 sn  |
| Gitleaks Secrets Scan   | failure     | 2 sn  |
| Semgrep Scan            | failure     | 2 sn  |
| build                   | failure     | 3 sn  |
| Security Scan           | failure     | 3 sn  |
| sync                    | failure     | 3 sn  |
| Visual Regression Lock  | failure     | 3 sn  |
| Trigger Jules Agent     | failure     | 9 sn  |
| Trivy Scan              | failure     | 11 sn |
| PR Preview              | failure     | 11 sn |
| Vercel Preview Comments | **success** | 0 sn  |

13 kontrolün 12'si başarısız. Aynı imza PR #68 (12/13) ve PR #70 (14/15) için de raporlandı. 2–3 saniyede biten bir `build` işi hiçbir adım çalıştırmamıştır — bu bir test hatası değil, işin başlamadan düşmesidir.

### 1b. v13.1'in "master'da 30/30 başarısız" rakamı YANLIŞTI — Kural 10 öz-düzeltmesi

v13.1'de bir alt ajanın raporladığı "master'da son 30 koşunun 30'u başarısız" rakamı **doğrulanmadan doktrine yazıldı**. Doğrudan ölçüm (`mcp__github__actions_list`, `branch: master`, son 30 koşu, 2026-08-06):

**17 başarılı · 12 başarısız · 1 atlanmış.** Süreler 5–63 saniye. Adımlar gerçekten çalışıyor, `steps` dizileri dolu.

Bu turda test edilen **fatura/kota hipotezi de çürüdü**: depo private yapıldığı için Actions kotasının dolduğu ve runner'ın hiç atanmadığı öne sürülmüştü. Ama `master`'da işler tam adım listesiyle koşuyor ve bugün başarılı koşular var — kota dolmuş bir hesapta bu mümkün değildir.

### 1c. Doğru tablo: iki ayrı sorun, v13.1 bunları karıştırdı

**Sorun 1 — `master` (kısmi, gerçek hatalar).** Üç workflow düşüyor ve hepsi altyapı değil kod/config kaynaklı:

| Workflow         | Düştüğü adım                                              | Sınıf                                       |
| ---------------- | --------------------------------------------------------- | ------------------------------------------- |
| `CI`             | "i18n Key Check"                                          | eksik/uyumsuz çeviri anahtarı               |
| `Release`        | release-please → "Resource not accessible by integration" | token izni (`permissions: contents: write`) |
| `Security Audit` | —                                                         | incelenecek                                 |

Buna karşılık `Plan Guard`, `Agent Routing Guard`, `Sync Architect Decisions`, `Automated Secret Scan`, `Test Runner` **başarılı** — yani yaptırım katmanı çalışıyor.

**Sorun 2 — PR bağlamı (neredeyse tam kapalı).** PR #69'da 13 kontrolün 12'si 2–3 saniyede, adım çalıştırmadan düşüyor (§1a tablosu, doğrudan doğrulandı). En güçlü açıklama **sır erişimi**: fallback veren tek workflow (`preview.yml`, `secrets.X || 'default'`) PR'da çalışan tek workflow; fallback'siz olanlar (`ci.yml`, `security.yml`, `secret-scan.yml` → `secrets.GITHUB_TOKEN`) anında düşüyor. Dependabot PR'ları tanımı gereği repo secret'ı almaz. `pull_request_target` hiçbir workflow'da kullanılmıyor.

Bu, v13.1'de ilk kurulan ama yanlış veriyle terk edilen hipotezdir; doğru veriyle **yeniden geçerlidir**.

### 1d. Yöntem dersi — kanıt zinciri

v13.1'de zincir şöyle yürüdü: hipotez (PR sırları) → **hatalı alt ajan verisiyle** terk edildi → fatura hipotezi → çürütüldü → ilk hipoteze dönüldü. Kaybedilen tur, doğrulanmamış bir sayının doktrine girmesinden kaynaklandı. Kural olarak yazıldı (MASTER_PLAN §3): **alt ajan çıktısı kanıt değil, kanıt adayıdır; doktrine giren her sayı çağıran tarafından doğrudan ölçülür.**

## 2. Kural 10 öz-düzeltmesi — G-8'deki kök-neden hipotezi çürüdü

`CLAUDE.md` G-8'de şu yazılıydı: _"repo/org düzeyinde bu `claude/*` branch paternine özel bir Actions kısıtlaması olduğu düşünülüyor."_ Kanıt olarak `plan-branch-sync` denemelerinin ~6-7 saniyede çöküşü gösterilmişti.

**Bu hipotez yanlıştır.** Aynı başarısızlık imzası `claude/*` ile hiç ilgisi olmayan `dependabot/*` branch'lerinin PR'larında ve 12 farklı workflow'da görülüyor. Başarısızlık branch paternine özgü değildir; **PR bağlamına** özgüdür. v12.129'da geri alınan `plan-branch-sync.yml` denemeleri de PR/otomasyon bağlamında koşuyordu — yani o turda alınan "revert" kararı doğruydu ama gerekçesi yanlıştı.

## 3. Yapısal sonuç — PR şeridi kapalı, ve doktrin o şeride iki ajan yerleştirmişti

`master` ruleset-korumalı: PR zorunlu + 7 status check. PR bağlamında kontroller koşamıyor ⇒ hiçbir PR merge edilemez.

Ölçülen: 4 açık PR — #68, #69, #70 (dependabot) ve #72 (`proje-360-derece-değerlendirme-20147`, insan-yazımı, Founder hesabından, 10 commit, 2 geri). Hiçbiri merge edilebilir değil.

MASTER_PLAN ZETA bloğu "Otopilot Aktif" işaretliydi ve ZETA-1 ajanlara "yalnızca PR aç" diyordu. `Trigger Jules Agent` kontrolü de başarısız. Yani iki ajan, fiziksel olarak kapalı olan tek şeride yerleştirilmişti. Düzeltme: ZETA-0 (P0) ön koşul olarak açıldı, ZETA bloğunun durumu BLOKLU'ya çevrildi.

**İkincil sonuç — yönetişim:** Anayasa (§3) doğrulama otoritesini CI'a devrediyor. CI kırmızıyken bu devir geçersizdir; kapanış kanıtı doğrudan ölçüm olmak zorundadır. Bu, MASTER_PLAN §3'e yazıldı.

## 4. Branch envanteri (origin, 2026-08-06)

| Branch                                     | Durum                                                     | Karar                                                    |
| ------------------------------------------ | --------------------------------------------------------- | -------------------------------------------------------- |
| `master`                                   | canlı, `f3c5481c`                                         | —                                                        |
| `claude/strategy-brief-review-i93xcv`      | `e6cc1755`, **0 ileri / 17 geri**, benzersiz commit'i yok | G-8 senkron ölçütü ihlal edildi; reset edilir, kayıp yok |
| `dependabot/...setup-node-7`               | PR #69, CI kırmızı                                        | ZETA-0 sonrası merge                                     |
| `dependabot/...upload-artifact-7`          | PR #68, CI kırmızı                                        | ZETA-0 sonrası merge                                     |
| `dependabot/...eslint-config-next-16.2.12` | PR #70, CI kırmızı                                        | ZETA-0 sonrası merge                                     |
| `proje-360-derece-değerlendirme-20147`     | PR #72, insan-yazımı, 10 commit                           | **Founder kararı bekliyor**                              |

> Not: ilk keşif ajanı `claude/...` dalını "17 ileri / 0 geri" raporladı. `git rev-list --left-right --count origin/master...origin/claude/...` çıktısı `17 0`'dır ve sol taraf **master**'ın fazlasıdır. Doğrusu: dal 17 **geri**, benzersiz commit'i yok. Ajan çıktısı doğrudan ölçümle düzeltildi.

## 5. Qwen "Quantum Sovereign v3.0" değerlendirmesi

Karar MASTER_PLAN §4a'dadır. Reddedilme gerekçeleri:

- **Var olmayan bir kısıt üzerine kurulu.** Plan "8GB RAM" ve yerel Qwen-1.5B router öneriyor; proje Vercel serverless + Supabase üzerinde çalışıyor, böyle bir bellek darboğazı yok.
- **Zaten inşa edilmiş bileşenleri yeniden öneriyor.** #B102 (hibrit model yönlendirme) → `callWithFailover()` + 9 adaptör canlı. #B301 (işlem bazlı gelir) → Stripe canlı, katmanlı API anahtarları mevcut. #B302 (Investor Dashboard) → `/investor-portal` canlı.
- **Build-trap'e geri dönüş.** MASTER_PLAN'ın kendi 5. satırı "build-trap dönemi bitmiştir" diyor; 5 haftalık yeni inşa planı ölçülen darboğazı (dağıtım, sıfır dış kullanıcı) çözmez.
- **113 dil** iddiası, 5 dilin çeviri kapsamı ~%45 iken hedef olamaz (v12.92 ölçümü).

**Kabul edilen tek fikir — #B201 "Executable Policies":** EU AI Act maddelerinin çalıştırılabilir kod blokları olarak tanımlanması. Gerçekten farklılaştırıcı ve belgenin kendi "araç değil standart" konumlandırmasıyla uyumlu. **Lansman sonrası aday** olarak kaydedildi; moratoryum gereği panele satır olarak eklenmedi.
